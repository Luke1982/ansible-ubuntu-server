# Webmail password guessing. Roundcube is configured to log to syslog (see
# configure-roundcube.yml), so the failed logins are read from the journal.
[roundcube-auth]
enabled  = true
backend  = systemd
port     = http,https
maxretry = 5
findtime = 600
bantime  = -1
